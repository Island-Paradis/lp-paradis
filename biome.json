{
  "$schema": "https://biomejs.dev/schemas/2.2.0/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "ignoreUnknown": true,
    "includes": [
      "**",
      "!node_modules",
      "!.next",
      "!dist",
      "!build",
      "!openspec"
    ]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "suspicious": {
        "noUnknownAtRules": "off"
      },
      "style": {
        "noRestrictedImports": {
          "level": "error",
          "options": {
            "paths": {
              "motion/react": {
                "importNames": ["useReducedMotion"],
                "message": "Use `useReducedMotion` de `@/lib/use-reduced-motion`. O hook do `motion` le a preferencia dentro de um inicializador de `useState`, e o valor difere entre servidor (`null`) e cliente sob `prefers-reduced-motion` (`true`) — divergencia de hidratacao em toda carga com a preferencia ativa. Ver a invariante em `openspec/specs/hydration-integrity`. Preferencia que so muda apresentacao vai para CSS; a que muda comportamento vai para o hook do projeto."
              }
            }
          }
        }
      }
    },
    "domains": {
      "next": "recommended",
      "react": "recommended"
    }
  },
  "assist": {
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  }
}
